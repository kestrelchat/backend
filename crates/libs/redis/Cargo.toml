[package]
name = "kestrel_redis"
version = "0.1.0"
edition = "2024"

[dependencies]
kestrel_common = { version = "0.1.0", path = "../common" }
redis = { version = "1.2.1", features = ["tokio-comp", "connection-manager"] }
serde_json = "1"
thiserror = "2.0.18"
