[package]
name = "kestrel_postgres"
version = "0.1.0"
edition = "2024"

[dependencies]
chrono = { workspace = true }
kestrel_common = { workspace = true }
sqlx = { workspace = true, features = ["postgres", "runtime-tokio-native-tls"] }
thiserror = { workspace = true }
ulid = { workspace = true }
